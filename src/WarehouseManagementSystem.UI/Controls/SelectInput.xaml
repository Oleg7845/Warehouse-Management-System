<UserControl x:Class="WarehouseManagementSystem.UI.Controls.SelectInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:WarehouseManagementSystem.UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="40"
             d:DesignWidth="300"
             x:Name="parent">

    <UserControl.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <conv:InverseBoolConverter x:Key="InverseBoolConverter" />

        <DataTemplate
            x:Key="DefaultItemTemplate">
            
            <TextBlock
                Text="{Binding}"
                FontSize="16"
                Foreground="{StaticResource Gray}"
                VerticalAlignment="Center" />
        </DataTemplate>

    </UserControl.Resources>

    <Border
        BorderBrush="{StaticResource Gray}"
        BorderThickness="1"
        CornerRadius="4"
        Background="{StaticResource Smoke}">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image
                x:Name="leftIcon"
                Grid.Column="0"
                Source="{Binding LeftIcon, ElementName=parent}"
                Width="18"
                Height="18"
                Margin="12,0,5,0"
                Stretch="Uniform"
                RenderOptions.BitmapScalingMode="HighQuality"
                Visibility="{Binding LeftIcon, ElementName=parent, Converter={StaticResource NullToVisibilityConverter}}" />

            <Grid
                Grid.Column="1">

                <ComboBox
                    x:Name="ComboBox"
                    ItemsSource="{Binding ItemsSource, ElementName=parent}"
                    SelectedItem="{Binding SelectedItem, ElementName=parent, Mode=TwoWay}"
                    ItemTemplate="{Binding ItemTemplate, ElementName=parent}"
                    IsEditable="False"
                    IsEnabled="{Binding IsReadOnly, ElementName=parent, Converter={StaticResource InverseBoolConverter}}"
                    FontSize="16"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{StaticResource Gray}"
                    VerticalContentAlignment="Center"
                    HorizontalContentAlignment="Stretch"
                    Padding="5,0,0,0">

                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            <Setter Property="ItemTemplate" Value="{StaticResource DefaultItemTemplate}" />

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBox">
                                        <Grid>
                                            <ToggleButton 
                                                Background="Transparent"
                                                BorderThickness="0"
                                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                Focusable="False"
                                                ClickMode="Press">

                                                <ToggleButton.Template>
                                                    <ControlTemplate TargetType="ToggleButton">

                                                        <Grid Background="Transparent">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="30" />
                                                            </Grid.ColumnDefinitions>

                                                            <Path 
                                                                Grid.Column="1"
                                                                Data="M 0 0 L 4 4 L 8 0"
                                                                Stroke="{StaticResource Gray}"
                                                                StrokeThickness="2"
                                                                VerticalAlignment="Center"
                                                                HorizontalAlignment="Center" />
                                                        </Grid>
                                                    </ControlTemplate>
                                                </ToggleButton.Template>
                                            </ToggleButton>

                                            <ContentPresenter 
                                                IsHitTestVisible="False"
                                                Content="{TemplateBinding SelectionBoxItem}"
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                Margin="10,0,30,0"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left">

                                                <ContentPresenter.Resources>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource Gray}" />
                                                        <Setter Property="FontSize" Value="16" />
                                                    </Style>
                                                </ContentPresenter.Resources>
                                            </ContentPresenter>

                                            <Popup 
                                                x:Name="Popup"
                                                Placement="Bottom"
                                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                                AllowsTransparency="True"
                                                Focusable="False"
                                                PopupAnimation="Slide"
                                                MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">

                                                <Grid 
                                                    x:Name="DropDown"
                                                    SnapsToDevicePixels="True"
                                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">

                                                    <Border 
                                                        x:Name="DropDownBorder"
                                                        Background="{StaticResource Smoke}"
                                                        BorderThickness="1"
                                                        BorderBrush="{StaticResource Gray}"
                                                        CornerRadius="0,0,4,4"
                                                        Margin="0,1,0,0" />

                                                    <ScrollViewer Margin="0,2" SnapsToDevicePixels="True">
                                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                                    </ScrollViewer>
                                                </Grid>
                                            </Popup>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Style>

                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">

                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding" Value="10,8" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="Foreground" Value="{StaticResource Gray}" />
                            <Setter Property="Template">

                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">

                                        <Border
                                            x:Name="ItemBorder"
                                            Background="{TemplateBinding Background}">

                                            <ContentPresenter 
                                                Margin="{TemplateBinding Padding}"
                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                RecognizesAccessKey="True" />
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsHighlighted" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource Green}" />
                                                <Setter Property="Foreground" Value="White" />
                                            </Trigger>

                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource Green}" />
                                                <Setter Property="Foreground" Value="White" />
                                                <Setter Property="FontWeight" Value="SemiBold" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>

                <TextBlock
                    IsHitTestVisible="False"
                    Text="{Binding Placeholder, ElementName=parent}"
                    Foreground="{StaticResource LightGray}"
                    FontSize="16"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Margin="10,0,30,0">

                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedItem, ElementName=parent}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Grid>
    </Border>
</UserControl>
