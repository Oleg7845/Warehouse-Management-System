<UserControl
    x:Class="WarehouseManagementSystem.UI.Pages.SupportTickets.SupportTicketCreate.SupportTicketCreateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:WarehouseManagementSystem.UI.Pages.SupportTickets.SupportTicketCreate"
    xmlns:controls="clr-namespace:WarehouseManagementSystem.UI.Controls"
    xmlns:conv="clr-namespace:WarehouseManagementSystem.UI.Converters"
    mc:Ignorable="d" 
    Background="Transparent">

    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Border
        Padding="20"
        CornerRadius="16"
        ClipToBounds="True"
        Background="{StaticResource White}">

        <Grid
            HorizontalAlignment="Left">

            <StackPanel
            Width="300">

                <TextBlock
                    Margin="0,0,0,20"
                    HorizontalAlignment="Center"
                    Text="Create Support Request"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="{StaticResource Gray}" />

                <controls:TextInput
                    Height="40"
                    Margin="0,0,0,15"
                    Placeholder="Username"
                    LeftIcon="pack://application:,,,/WarehouseManagementSystem.UI;component/Resources/Icons/user.png"
                    Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                    Visibility="{Binding IsGuestMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <controls:TextInput
                    Height="40"
                    Margin="0,0,0,15"
                    Placeholder="Title"
                    LeftIcon="pack://application:,,,/WarehouseManagementSystem.UI;component/Resources/Icons/tag.png"
                    Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />

                <controls:TextInput
                    Height="40"
                    Margin="0,0,0,15"
                    Placeholder="Description"
                    LeftIcon="pack://application:,,,/WarehouseManagementSystem.UI;component/Resources/Icons/edit.png"
                    Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />

                <controls:SelectInput
                    Height="40"
                    Margin="0,0,0,20"
                    ItemsSource="{Binding AvailablePriorities}"
                    SelectedItem="{Binding SelectedPriority}"
                    Placeholder="Priority"
                    LeftIcon="pack://application:,,,/WarehouseManagementSystem.UI;component/Resources/Icons/flag.png" />

                <controls:SolidButton
                    Width="300"
                    Height="40"
                    FontSize="14"
                    Background="{StaticResource Green}"
                    CornerRadius="8"
                    Text="Send request"
                    LeftIcon="pack://application:,,,/WarehouseManagementSystem.UI;component/Resources/Icons/email.png"
                    IconColor="{StaticResource White}"
                    Command="{Binding SendRequestCommand}"
                    IsDefault="True" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
