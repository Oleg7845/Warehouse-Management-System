<Window
    x:Class="WarehouseManagementSystem.UI.Pages.Main.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:WarehouseManagementSystem.UI.Pages.Main"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d"
    SizeToContent="WidthAndHeight"
    WindowStartupLocation="CenterScreen"
    Title="Warehouse Management System (WMS)">

    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="ResizeMode" Value="NoResize"/>

            <Style.Triggers>
                <DataTrigger Binding="{Binding CanResize}" Value="True">
                    <Setter Property="ResizeMode" Value="CanResize"/>
                    <Setter Property="SizeToContent" Value="Manual"/>
                    <Setter Property="Width" Value="1200"/>
                    <Setter Property="Height" Value="800"/>
                </DataTrigger>

                <DataTrigger Binding="{Binding CanResize}" Value="False">
                    <Setter Property="ResizeMode" Value="NoResize"/>
                    <Setter Property="SizeToContent" Value="WidthAndHeight"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>

    <Grid>
        <materialDesign:DialogHost
            Identifier="RootDialogHost"
            CloseOnClickAway="True">

            <DockPanel>
                <ContentControl
                    x:Name="MainContent"
                    Content="{Binding CurrentView}" />
            </DockPanel>
        </materialDesign:DialogHost>

        <materialDesign:Snackbar 
            x:Name="MainSnackbar"
            MessageQueue="{Binding SnackbarMessageQueue}"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Background="Transparent"
            materialDesign:ShadowAssist.ShadowDepth="Depth0">

            <materialDesign:Snackbar.Resources>
                <Style TargetType="materialDesign:SnackbarMessage">
                    <Setter Property="Background" Value="{Binding SnackbarBackground}" />
                    <Setter Property="Foreground" Value="{Binding SnackbarForeground}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="materialDesign:SnackbarMessage">
                                <Border
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8"
                                    Padding="20,12,20,14">
                                    <ContentPresenter
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </materialDesign:Snackbar.Resources>
        </materialDesign:Snackbar>
    </Grid>
</Window>
